## Packages
framer-motion | Page transitions and smooth micro-interactions
lucide-react | High-quality icons for the UI

## Notes
The application uses the Replit AI integrations to generate explanations. 
The explanation generation endpoint is a POST request, so we trigger a mutation on page load and when changing reading levels.
